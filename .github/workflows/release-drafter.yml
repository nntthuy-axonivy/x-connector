name: Release Drafter

on:
  push:
***REMOVED***branches:
***REMOVED***  - master
  pull_request:
***REMOVED***types: [opened, reopened, synchronize]

permissions:
  contents: write
  pull-requests: write

jobs:
  build:
***REMOVED***uses: axonivy-market/github-workflows/.github/workflows/release-drafter.yml@v4
